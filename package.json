{
  "name": "btr-marketing-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "db:push": "npx prisma db push",
    "db:studio": "npx prisma studio",
    "db:seed": "curl -X POST http://localhost:3000/api/seed",
    "postinstall": "npx prisma generate && cd client && npm install"
  },
  "dependencies": {
    "@prisma/client": "^6.8.0",
    "@vercel/blob": "^0.27.0",
    "@vercel/node": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^6.8.0"
  }
}
